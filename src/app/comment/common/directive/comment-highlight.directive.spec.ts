import { CommentHighlightDirective } from './comment-highlight.directive';

describe('CommentHighlightDirective', () => {
  /*it('should create an instance', () => {
    const directive = new CommentHighlightDirective();
    expect(directive).toBeTruthy();
  });*/
});
